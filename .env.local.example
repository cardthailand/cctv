# คัดลอกค่า DB ด้านล่างไปใส่ใน .env สำหรับพัฒนาบนเครื่อง local
# รัน Postgres: docker compose -f docker-compose.dev.yml up -d

DB_HOST=localhost
DB_PORT=5433
DB_NAME=cctv_db
DB_USER=cctv_db
DB_PASSWORD=dev_local_password
DB_SSL=false
